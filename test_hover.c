#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    // Test hover documentation on standard library functions
    printf("Hello, World!\n");  // Hover over printf
    malloc(100);               // Hover over malloc  
    strlen("test");            // Hover over strlen
    return 0;
}